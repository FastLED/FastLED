name: adafruit_xiaoblesense_nRF52840

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master


jobs:
  build:
    uses: ./.github/workflows/build_template.yml
    with:
      args: xiaoblesense_adafruit
