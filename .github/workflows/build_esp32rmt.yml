name: esp32rmt_51

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  build:
    uses: ./.github/workflows/build_template.yml
    with:
      args: esp32rmt_51 --examples Esp32Rmt51 --defines FASTLED_RMT5=1
