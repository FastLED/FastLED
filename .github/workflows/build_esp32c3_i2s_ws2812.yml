name: esp32c3_i2s_ws2812

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  build:
    uses: ./.github/workflows/build_template.yml
    with:
      args: --boards esp32-c3-devkitm-1 --defines FASTLED_ESP32_I2S