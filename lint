#!/bin/bash

set -e
cd "$(dirname "$0")"


./ci/lint


uv run clang-format -i src/lib8tion/*.h
uv run clang-format -i src/stubs/*.h
uv run clang-format -i src/stubs/*.cpp
uv run clang-format -i src/platforms/arm/**/*.h
uv run clang-format -i src/platforms/arm/**/*.cpp
