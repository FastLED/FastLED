# Docker build context exclusions for FastLED compiler Docker images
# Reduces context size and speeds up Docker builds

# Version control
**/.git
**/.gitignore
**/.gitattributes

# Python cache and bytecode (will be generated during build)
**/__pycache__
**/*.pyc
**/*.pyo
**/*.pyd
**/.Python
**/.pytest_cache
**/.mypy_cache
**/.ruff_cache

# Node.js
**/node_modules
**/npm-debug.log
**/yarn-error.log

# Build artifacts (will be generated during build)
**/.build
**/.pio
**/build
**/dist
**/*.o
**/*.a
**/*.so
**/*.elf
**/*.bin
**/*.hex

# IDE and editor files
**/.vscode
**/.idea
**/*.swp
**/*.swo
**/~*

# OS files
**/.DS_Store
**/Thumbs.db
**/*.tmp

# Documentation (not needed in Docker builds)
**/*.md
!README.md

# Test files and fixtures (not needed for compilation)
tests/
examples/

# CI/CD configs (not needed in Docker)
.github/

# Temporary files
**/tmp
**/*.log

# UV virtual environment
.venv/
venv/

# FastLED source (will be cloned fresh in Docker)
src/
*.h
*.cpp
!ci/**
