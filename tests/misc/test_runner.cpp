// Test runner - DEPRECATED
// This test was for the old category-based DLL system
// The build system now builds individual test DLLs instead

#include "test.h"

FL_TEST_CASE("Test runner - deprecated") {
    // This test is deprecated and no longer needed
    // Tests are now built as individual DLLs, not category DLLs
    FL_WARN("test_runner.cpp is deprecated - tests are now individual DLLs");
}
