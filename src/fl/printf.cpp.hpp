#pragma once

// Template implementations for fl::printf
// All implementations are now in the header file printf.h
// since template functions must be defined where they're declared.

#include "fl/printf.h"

// This file intentionally left mostly empty since template 
// implementations have been moved to the header.
