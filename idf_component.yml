## IDF Component Manager dependencies for FastLED
## Used by PlatformIO (pioarduino) to pull ESP-IDF components during build.
dependencies:
  espressif/esp_h264:
    version: "^1.2.0"
    rules:
      - if: "target == esp32p4"
