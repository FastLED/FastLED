# Cursor Rules Configuration
# This file defines rules and hooks for the Cursor editor

# Run linting after every code change
post_change_hooks:
  - command: "bash lint"
    description: "Run code formatting and linting"
    working_directory: "."

# Coding Guidelines
coding_guidelines:
  # C++ Code Patterns
  - name: "Shared Implementation Pattern"
    description: "When writing a lot of code that applies the same operation on a bunch of C++ objects, try and determine if those objects share a common base class or internal object. If so consider whether it's appropriate to move the implementation into a shared space."
    category: "C++ Design Patterns"
